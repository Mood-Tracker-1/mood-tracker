

const initialState = {
    user: null,
    cardsList: [],
    lastCardId: 1,
  };

const cardsReducer = (state = initialState, action) => {
    let cardsList;
    
    switch (action.type) {
      case types.ADD_CARD: {
          //increment 
  
      }
    };
  };